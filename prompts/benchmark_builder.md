You are generating benchmark prompts for evaluating Small Language Models.

Behaviour:

{BEHAVIOUR_DEFINITION}

Examples:

{SEED_PROMPTS}

Generate exactly {NUM_VARIATIONS} NEW user prompts that follow the behaviour definition.

Do not rewrite the examples.

Return ONLY valid JSON.

[
{
"prompt": "..."
}
]
