[project]
name = "behaviourlab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "dotenv>=0.9.9",
    "google-genai>=2.10.0",
    "sentence-transformers>=5.6.0",
]
